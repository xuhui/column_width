<style>
  <% idx = @project ? @project.cw_index1 : 0 %>
  <% wth = @project ? @project.cw_value1 : 0 %>
  <% if idx > 0 && wth > 0 %>
    table.list th:nth-child(<%= "#{idx}" %>) { min-width: <%= "#{wth}" %>px;  max-width: <%= "#{wth}" %>px; }
  <% end%>
  table.list tbody tr td.cf_19.string {word-break: break-all; white-space: normal;}
  table.list tbody tr td.author {white-space: normal;}
</style>
